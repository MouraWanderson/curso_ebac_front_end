function calculaArea(valorArea) {
    let base = prompt("valor base: ");
    let altura = prompt("Valor altura");
    return base * altura
}

alert("O valor da área digitada é igual a " + calculaArea())
